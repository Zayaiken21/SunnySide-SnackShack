# Sunny Side Snack Shack — Emergency Stable Build

This is the load-safe rollback build.

What this fixes:
- Restores the game so it loads again.
- Keeps the working menu click behavior.
- Prevents decorative layers from blocking buttons.
- Keeps the compact pause button inside the border.
- Keeps the game inside the phone viewport.
- Keeps the stable multiplayer server structure.

Upload these files to GitHub and let Render redeploy.

Render:
Build command: npm install
Start command: npm start
